#include <stdio.h>

/**
 * main - a program that prints single-digit numbers
 * Return:0 (success)
 */
int main(void)
{
int j;
for (j = 0; j < 10; j++)
putchar(n);
if (j != 10)
putchar(',');
putchar(' ');
putchar('\n');
return (0);
}
