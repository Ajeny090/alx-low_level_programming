#include <stdio.h>

/**
 * main - a program that prints all single digit numbers
 * Return:0 (Success)
 */
int main(void)
{
for (int i = 0; i < 10; i++)
putchar(i + '0');
putchar('\n');
return (0);
}
